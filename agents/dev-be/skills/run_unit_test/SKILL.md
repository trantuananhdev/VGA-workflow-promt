# skill_run_unit_test (Dev-BE)

**Mục tiêu:** Chạy unit test cho đúng phạm vi story đang xử lý trước khi emit handoff sang `qa`.

## STACK BINDING — điền khi chọn công nghệ backend

```
<TODO: điền lệnh test thật, vd `npm test`, `pytest`, `go test ./...`>
```

**Verify:** toàn bộ test liên quan story phải pass, đính kèm log thật vào handoff envelope
(`kernel/mailbox/HANDOFF-*.md`) — không tóm tắt bằng lời "tests pass".
