# store-keyword-blocklist.md — Layer 1, chỉ nạp khi `skill_check_app_store_policy` chạy

Danh sách từ khoá/nội dung từng gây reject hoặc cần tránh, cập nhật dần theo kinh nghiệm thực tế
(mỗi lần bị reject vì lý do mới → thêm vào đây + ghi vào `shared/lessons_learned.md`).

## Apple App Store
- <cập nhật khi có case thật>

## Google Play
- <cập nhật khi có case thật>

> File này CHƯA có dữ liệu thật — cần điền khi bắt đầu submit app đầu tiên. Đừng để trống mãi,
> vì `skill_check_app_store_policy` sẽ không phát hiện được gì nếu blocklist rỗng.
